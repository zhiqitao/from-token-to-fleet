# ---- fig-10-1001: five parallelization strategies ----
# DISABLED: redrawn with Archify (render/archify/ch10-parallel.json + ch10-parallel-min.html)
# so each of the five strategies is rendered as its own semantic node type (weight matrix /
# layer stack / replicated model / MoE experts / context window), each splitting into its GPU
# shards -- not five identical boxes. Asset: chapter-10/figures/fig-10-1001.{pdf,png}.
